# Backend environment variables (example)

# General
NODE_ENV=development
PORT=3001

# PostgreSQL connection string
# Replace username, password, host, port and database name as needed
DATABASE_URL="postgresql://postgres:password@localhost:5432/ukkis?schema=public"

# Stripe (optional – needed only if you test real payment intents)
# Generate test keys from your Stripe Dashboard
STRIPE_SECRET_KEY=sk_test_your_stripe_test_key_here
# If you add a webhook route later, set the signing secret here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

